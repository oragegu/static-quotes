server {
    listen 80;
    server_name localhost;

    location /images {
        root /usr/share/nginx/html;
    }

    location / {
        root /usr/share/nginx/html/pages;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
